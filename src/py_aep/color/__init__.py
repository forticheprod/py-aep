"""Color-management helpers: profile-envelope codec, ICC discovery, OCIO config.

Pure leaf package (depends only on `enums/`, stdlib, and `yaml`); imported by
`models/` for reading and writing AE color-management settings.
"""
